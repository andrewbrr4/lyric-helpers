# lyric-helpers

A repo for various jupyter notebooks, functions and classes with the common theme of using NLP/AI technologies to assist with song lyric writing.

Uses the conda environments `lyrics-helpers`.